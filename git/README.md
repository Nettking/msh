# `git/` folder (unclear historical material)

This folder currently contains a `.gitignore` listing names of files that are **not present here** (e.g., `ig_recorder.py`, `record_data.py`, `standalone_recorder.py`).

Interpretation is uncertain, but it likely represents legacy notes or an older staging area. To avoid breaking unknown external references, this repository currently keeps the folder unchanged.

If this folder is confirmed obsolete, move or archive it in a future dedicated cleanup change.
