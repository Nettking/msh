# `record data/`

This directory is asset-only.

## Contents

- `button.png` — template image used by the auto-click utility.

## Moved Python scripts

Python scripts that previously lived here now live in `catalog/`:

- `catalog/standalone_recorder/standalone_recorder.py` (legacy recorder)
- `catalog/standalone-recorder_v2/standalone-recorder_v2.py` (active recorder)
- `catalog/auto_connect/auto_connect.py` (legacy desktop utility)

See `catalog/README.md` for status and per-script documentation links.
