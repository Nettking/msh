name: Apply exact Phase E identity fix

on:
  pull_request:
    branches: [main]
    types: [synchronize]
    paths:
      - ".phase-e-fix.py"
      - ".github/workflows/phase-e-fix.yml"

permissions:
  contents: write

concurrency:
  group: phase-e-fix-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}
      - name: Apply verified replacement
        run: python .phase-e-fix.py
      - name: Commit source correction
        shell: bash
        run: |
          set -euo pipefail
          git rm .phase-e-fix.py
          git add catalog/federation/former_primary_repair.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Fix Phase E immutable identity validation"
          git push origin HEAD:agent/complete-phase-e-recovery
