name: Temporary federation control-command refactor

on: [workflow_dispatch]

permissions:
  contents: write

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary validation workflow is staged manually."
