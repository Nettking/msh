name: Temporary federation control-command refactor

on:
  push:
    branches:
      - agent/federation-control-command-primitives

permissions:
  contents: write

jobs:
  refactor-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Apply bounded control-command refactor
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          core = Path("catalog/federation/control_commands.py")
          if core.exists():
              raise SystemExit("control_commands.py already exists")
          core.write_text('''"""Shared wire-level primitives for Federation control-plane commands.

This module deliberately stops at command metadata and wire validation.  It is
not a scheduler, job store, transport, authorization policy, or executor.
Feature-specific commands retain their existing schemas and execution semantics.
"""

from __future__ import annotations

import hashlib
import json
from dataclasses import dataclass
from datetime import datetime, timedelta, timezone
from typing import Mapping, Sequence

MAX_COMMAND_REQUEST_ID_LENGTH = 128
MAX_COMMAND_NODE_ID_LENGTH = 512
MAX_COMMAND_TARGETS = 256
COMMAND_CLOCK_SKEW = timedelta(minutes=1)


def stamp_utc(value: datetime) -> str:
    """Serialize an aware timestamp in the existing Federation UTC wire form."""

    if value.tzinfo is None or value.utcoffset() is None:
        raise ValueError("malformed_timestamp")
    return value.astimezone(timezone.utc).isoformat().replace("+00:00", "Z")


def parse_utc_stamp(value: object) -> datetime:
    """Parse one aware Federation timestamp and normalize it to UTC."""

    if not isinstance(value, str):
        raise TypeError("malformed_timestamp")
    try:
        parsed = datetime.fromisoformat(value.replace("Z", "+00:00"))
    except ValueError as exc:
        raise ValueError("malformed_timestamp") from exc
    if parsed.tzinfo is None or parsed.utcoffset() is None:
        raise ValueError("malformed_timestamp")
    return parsed.astimezone(timezone.utc)


def ensure_bounded_json(value: object, *, max_bytes: int, error_code: str) -> None:
    """Apply the existing canonical JSON byte-size guard to a wire payload."""

    encoded = json.dumps(
        value,
        sort_keys=True,
        separators=(",", ":"),
        ensure_ascii=False,
        allow_nan=False,
    ).encode("utf-8")
    if len(encoded) > max_bytes:
        raise ValueError(error_code)


def correlated_event_request_id(prefix: str, request_id: str, node_id: str) -> str:
    """Return the stable bounded event request ID used by control-plane reports."""

    digest = hashlib.sha256(f"{request_id}\\0{node_id}".encode()).hexdigest()[:32]
    return f"{prefix}-{digest}"


def _request_id(value: object) -> str:
    if (
        not isinstance(value, str)
        or not value
        or len(value) > MAX_COMMAND_REQUEST_ID_LENGTH
    ):
        raise ValueError("malformed_request_id")
    return value


def _targets(
    value: object,
    *,
    max_targets: int,
    deduplicate: bool,
    require_unique: bool,
) -> tuple[str, ...]:
    if not isinstance(value, (list, tuple)):
        raise ValueError("malformed_targets")
    targets = tuple(value)
    if any(
        not isinstance(item, str)
        or not item
        or len(item) > MAX_COMMAND_NODE_ID_LENGTH
        for item in targets
    ):
        raise ValueError("malformed_targets")
    if deduplicate:
        targets = tuple(dict.fromkeys(targets))
    if not 1 <= len(targets) <= max_targets:
        raise ValueError("malformed_targets")
    if require_unique and len(set(targets)) != len(targets):
        raise ValueError("malformed_targets")
    return targets


@dataclass(frozen=True)
class ControlCommandEnvelope:
    """Common metadata carried by bounded Federation control commands."""

    request_id: str
    target_node_ids: tuple[str, ...]
    created_at: datetime
    expires_at: datetime

    @classmethod
    def issue(
        cls,
        *,
        request_id: str,
        target_node_ids: Sequence[str],
        created_at: datetime,
        expires_at: datetime,
        max_lifetime: timedelta,
        max_targets: int = MAX_COMMAND_TARGETS,
    ) -> "ControlCommandEnvelope":
        request = _request_id(request_id)
        targets = _targets(
            tuple(target_node_ids),
            max_targets=max_targets,
            deduplicate=True,
            require_unique=False,
        )
        created = _aware_utc(created_at)
        expires = _aware_utc(expires_at)
        if expires <= created or expires - created > max_lifetime:
            raise ValueError("invalid_lifetime")
        return cls(request, targets, created, expires)

    @classmethod
    def parse_payload(
        cls,
        value: Mapping[str, object],
        *,
        max_lifetime: timedelta,
        max_targets: int = MAX_COMMAND_TARGETS,
        require_unique_targets: bool,
        now: datetime | None = None,
    ) -> "ControlCommandEnvelope":
        request = _request_id(value.get("request_id"))
        targets = _targets(
            value.get("target_node_ids"),
            max_targets=max_targets,
            deduplicate=False,
            require_unique=require_unique_targets,
        )
        created = parse_utc_stamp(value.get("created_at"))
        expires = parse_utc_stamp(value.get("expires_at"))
        reference = _aware_utc(now or datetime.now(timezone.utc))
        if (
            created > reference + COMMAND_CLOCK_SKEW
            or expires <= reference
            or expires <= created
            or expires - created > max_lifetime
        ):
            raise ValueError("expired_or_invalid_request")
        return cls(request, targets, created, expires)

    def payload_fields(self) -> dict[str, object]:
        return {
            "request_id": self.request_id,
            "target_node_ids": list(self.target_node_ids),
            "created_at": stamp_utc(self.created_at),
            "expires_at": stamp_utc(self.expires_at),
        }


def _aware_utc(value: datetime) -> datetime:
    if value.tzinfo is None or value.utcoffset() is None:
        raise ValueError("malformed_timestamp")
    return value.astimezone(timezone.utc)


__all__ = [
    "COMMAND_CLOCK_SKEW",
    "MAX_COMMAND_NODE_ID_LENGTH",
    "MAX_COMMAND_REQUEST_ID_LENGTH",
    "MAX_COMMAND_TARGETS",
    "ControlCommandEnvelope",
    "correlated_event_request_id",
    "ensure_bounded_json",
    "parse_utc_stamp",
    "stamp_utc",
]
''', encoding="utf-8")

          update = Path("catalog/flask_app/services/federation_update_events.py")
          text = update.read_text(encoding="utf-8")
          old = '''from catalog.federation.software_update import (\n    APPROVED_BRANCH,\n    APPROVED_REPOSITORY,\n    OID_RE,\n    UpdateInspection,\n)\n'''
          new = '''from catalog.federation.control_commands import (\n    ControlCommandEnvelope,\n    correlated_event_request_id as _event_request_id,\n    ensure_bounded_json,\n    stamp_utc as _stamp,\n)\nfrom catalog.federation.software_update import (\n    APPROVED_BRANCH,\n    APPROVED_REPOSITORY,\n    OID_RE,\n    UpdateInspection,\n)\n'''
          if text.count(old) != 1:
              raise SystemExit("update import shape changed")
          text = text.replace(old, new)

          old = '''def _stamp(value: datetime) -> str:\n    return value.astimezone(timezone.utc).isoformat().replace("+00:00", "Z")\n\n\ndef _parse_stamp(value: object) -> datetime:\n    if not isinstance(value, str):\n        raise TypeError("malformed_timestamp")\n    parsed = datetime.fromisoformat(value.replace("Z", "+00:00"))\n    if parsed.tzinfo is None or parsed.utcoffset() is None:\n        raise ValueError("malformed_timestamp")\n    return parsed.astimezone(timezone.utc)\n\n\ndef _bounded(value: object) -> None:\n    encoded = json.dumps(\n        value,\n        sort_keys=True,\n        separators=(",", ":"),\n        ensure_ascii=False,\n        allow_nan=False,\n    ).encode("utf-8")\n    if len(encoded) > MAX_EVENT_BYTES:\n        raise ValueError("update_event_too_large")\n'''
          new = '''def _bounded(value: object) -> None:\n    ensure_bounded_json(\n        value,\n        max_bytes=MAX_EVENT_BYTES,\n        error_code="update_event_too_large",\n    )\n'''
          if text.count(old) != 1:
              raise SystemExit("update helper shape changed")
          text = text.replace(old, new)

          old = '''    if not request_id or len(request_id) > 128:\n        raise ValueError("malformed_request_id")\n    if not OID_RE.fullmatch(target_commit):\n        raise ValueError("malformed_target")\n    targets = tuple(dict.fromkeys(target_node_ids))\n    if not targets or len(targets) > MAX_TARGETS:\n        raise ValueError("malformed_targets")\n    if any(\n        not isinstance(item, str) or not item or len(item) > 512\n        for item in targets\n    ):\n        raise ValueError("malformed_targets")\n    if expires_at <= created_at or (expires_at - created_at).total_seconds() > 900:\n        raise ValueError("invalid_lifetime")\n    value: dict[str, object] = {\n        "schema": EVENT_SCHEMA,\n        "request_id": request_id,\n        "repository": APPROVED_REPOSITORY,\n        "branch": APPROVED_BRANCH,\n        "target_commit": target_commit,\n        "target_node_ids": list(targets),\n        "created_at": _stamp(created_at),\n        "expires_at": _stamp(expires_at),\n    }\n'''
          new = '''    envelope = ControlCommandEnvelope.issue(\n        request_id=request_id,\n        target_node_ids=target_node_ids,\n        created_at=created_at,\n        expires_at=expires_at,\n        max_lifetime=timedelta(minutes=15),\n        max_targets=MAX_TARGETS,\n    )\n    if not OID_RE.fullmatch(target_commit):\n        raise ValueError("malformed_target")\n    value: dict[str, object] = {\n        "schema": EVENT_SCHEMA,\n        **envelope.payload_fields(),\n        "repository": APPROVED_REPOSITORY,\n        "branch": APPROVED_BRANCH,\n        "target_commit": target_commit,\n    }\n'''
          if text.count(old) != 1:
              raise SystemExit("update command_payload shape changed")
          text = text.replace(old, new)

          old = '''    request_id = value.get("request_id")\n    target = value.get("target_commit")\n    targets = value.get("target_node_ids")\n    if not isinstance(request_id, str) or not request_id or len(request_id) > 128:\n        raise ValueError("malformed_request_id")\n    if (\n        value.get("repository") != APPROVED_REPOSITORY\n        or value.get("branch") != APPROVED_BRANCH\n    ):\n        raise ValueError("unapproved_source")\n    if not isinstance(target, str) or not OID_RE.fullmatch(target):\n        raise ValueError("malformed_target")\n    if (\n        not isinstance(targets, list)\n        or not 1 <= len(targets) <= MAX_TARGETS\n        or any(\n            not isinstance(item, str) or not item or len(item) > 512\n            for item in targets\n        )\n    ):\n        raise ValueError("malformed_targets")\n    created = _parse_stamp(value.get("created_at"))\n    expires = _parse_stamp(value.get("expires_at"))\n    now = datetime.now(timezone.utc)\n    if (\n        created > now + timedelta(minutes=1)\n        or expires <= now\n        or (expires - created).total_seconds() > 900\n    ):\n        raise ValueError("expired_or_invalid_request")\n'''
          new = '''    envelope = ControlCommandEnvelope.parse_payload(\n        value,\n        max_lifetime=timedelta(minutes=15),\n        max_targets=MAX_TARGETS,\n        require_unique_targets=False,\n    )\n    target = value.get("target_commit")\n    if (\n        value.get("repository") != APPROVED_REPOSITORY\n        or value.get("branch") != APPROVED_BRANCH\n    ):\n        raise ValueError("unapproved_source")\n    if not isinstance(target, str) or not OID_RE.fullmatch(target):\n        raise ValueError("malformed_target")\n    del envelope\n'''
          if text.count(old) != 1:
              raise SystemExit("update validator shape changed")
          text = text.replace(old, new)

          old = '''def _event_request_id(prefix: str, request_id: str, node_id: str) -> str:\n    digest = hashlib.sha256(\n        f"{request_id}\\0{node_id}".encode()\n    ).hexdigest()[:32]\n    return f"{prefix}-{digest}"\n\n\n'''
          if text.count(old) != 1:
              raise SystemExit("update event request id shape changed")
          text = text.replace(old, "")
          update.write_text(text, encoding="utf-8")

          capability = Path("catalog/flask_app/services/federation_capability_requests.py")
          text = capability.read_text(encoding="utf-8")
          old = '''from catalog.federation.onboarding_models import (\n    BenchmarkState,\n    ContributionActivationState,\n    ContributionDesiredState,\n    ContributionPolicyState,\n)\n'''
          new = '''from catalog.federation.control_commands import (\n    ControlCommandEnvelope,\n    correlated_event_request_id as _event_request_id,\n    ensure_bounded_json,\n    parse_utc_stamp as _parse_stamp,\n    stamp_utc as _stamp,\n)\nfrom catalog.federation.onboarding_models import (\n    BenchmarkState,\n    ContributionActivationState,\n    ContributionDesiredState,\n    ContributionPolicyState,\n)\n'''
          if text.count(old) != 1:
              raise SystemExit("capability import shape changed")
          text = text.replace(old, new)

          old = '''def _stamp(value: datetime) -> str:\n    return value.astimezone(timezone.utc).isoformat().replace("+00:00", "Z")\n\n\ndef _parse_stamp(value: object) -> datetime:\n    if not isinstance(value, str):\n        raise TypeError("malformed_timestamp")\n    parsed = datetime.fromisoformat(value.replace("Z", "+00:00"))\n    if parsed.tzinfo is None or parsed.utcoffset() is None:\n        raise ValueError("malformed_timestamp")\n    return parsed.astimezone(timezone.utc)\n\n\ndef _bounded(value: object) -> None:\n    encoded = json.dumps(\n        value,\n        sort_keys=True,\n        separators=(",", ":"),\n        ensure_ascii=False,\n        allow_nan=False,\n    ).encode("utf-8")\n    if len(encoded) > MAX_EVENT_BYTES:\n        raise ValueError("capability_event_too_large")\n'''
          new = '''def _bounded(value: object) -> None:\n    ensure_bounded_json(\n        value,\n        max_bytes=MAX_EVENT_BYTES,\n        error_code="capability_event_too_large",\n    )\n'''
          if text.count(old) != 1:
              raise SystemExit("capability helper shape changed")
          text = text.replace(old, new)

          old = '''    if not request_id or len(request_id) > 128:\n        raise ValueError("malformed_request_id")\n    targets = tuple(dict.fromkeys(target_node_ids))\n    if not 1 <= len(targets) <= MAX_TARGETS:\n        raise ValueError("malformed_targets")\n    if any(\n        not isinstance(item, str) or not item or len(item) > 512\n        for item in targets\n    ):\n        raise ValueError("malformed_targets")\n    if expires_at <= created_at or expires_at - created_at > COMMAND_TTL:\n        raise ValueError("invalid_lifetime")\n    value: dict[str, object] = {\n        "schema": EVENT_SCHEMA,\n        "request_id": request_id,\n        "actions": list(REQUEST_ACTIONS),\n        "target_node_ids": list(targets),\n        "created_at": _stamp(created_at),\n        "expires_at": _stamp(expires_at),\n    }\n'''
          new = '''    envelope = ControlCommandEnvelope.issue(\n        request_id=request_id,\n        target_node_ids=target_node_ids,\n        created_at=created_at,\n        expires_at=expires_at,\n        max_lifetime=COMMAND_TTL,\n        max_targets=MAX_TARGETS,\n    )\n    value: dict[str, object] = {\n        "schema": EVENT_SCHEMA,\n        **envelope.payload_fields(),\n        "actions": list(REQUEST_ACTIONS),\n    }\n'''
          if text.count(old) != 1:
              raise SystemExit("capability request_payload shape changed")
          text = text.replace(old, new)

          old = '''    request_id = value.get("request_id")\n    targets = value.get("target_node_ids")\n    actions = value.get("actions")\n    if not isinstance(request_id, str) or not request_id or len(request_id) > 128:\n        raise ValueError("malformed_request_id")\n    if actions != list(REQUEST_ACTIONS):\n        raise ValueError("unsupported_actions")\n    if (\n        not isinstance(targets, list)\n        or not 1 <= len(targets) <= MAX_TARGETS\n        or len(set(targets)) != len(targets)\n        or any(\n            not isinstance(item, str) or not item or len(item) > 512\n            for item in targets\n        )\n    ):\n        raise ValueError("malformed_targets")\n    created = _parse_stamp(value.get("created_at"))\n    expires = _parse_stamp(value.get("expires_at"))\n    now = datetime.now(timezone.utc)\n    if (\n        created > now + timedelta(minutes=1)\n        or expires <= now\n        or expires <= created\n        or expires - created > COMMAND_TTL\n    ):\n        raise ValueError("expired_or_invalid_request")\n'''
          new = '''    ControlCommandEnvelope.parse_payload(\n        value,\n        max_lifetime=COMMAND_TTL,\n        max_targets=MAX_TARGETS,\n        require_unique_targets=True,\n    )\n    actions = value.get("actions")\n    if actions != list(REQUEST_ACTIONS):\n        raise ValueError("unsupported_actions")\n'''
          if text.count(old) != 1:
              raise SystemExit("capability validator shape changed")
          text = text.replace(old, new)

          old = '''def _event_request_id(prefix: str, request_id: str, node_id: str) -> str:\n    digest = hashlib.sha256(f"{request_id}\\0{node_id}".encode()).hexdigest()[:32]\n    return f"{prefix}-{digest}"\n\n\n'''
          if text.count(old) != 1:
              raise SystemExit("capability event request id shape changed")
          text = text.replace(old, "")
          capability.write_text(text, encoding="utf-8")

          tests = Path("catalog/federation/tests/test_control_commands.py")
          if tests.exists():
              raise SystemExit("test_control_commands.py already exists")
          tests.write_text('''from __future__ import annotations

from datetime import datetime, timedelta, timezone

import pytest

from catalog.federation.control_commands import (
    ControlCommandEnvelope,
    correlated_event_request_id,
    ensure_bounded_json,
    parse_utc_stamp,
    stamp_utc,
)


def test_issue_deduplicates_targets_without_changing_wire_fields() -> None:
    created = datetime(2026, 8, 12, 20, 0, tzinfo=timezone.utc)
    envelope = ControlCommandEnvelope.issue(
        request_id="request-one",
        target_node_ids=("node-a", "node-a", "node-b"),
        created_at=created,
        expires_at=created + timedelta(minutes=10),
        max_lifetime=timedelta(minutes=10),
    )

    assert envelope.payload_fields() == {
        "request_id": "request-one",
        "target_node_ids": ["node-a", "node-b"],
        "created_at": "2026-08-12T20:00:00Z",
        "expires_at": "2026-08-12T20:10:00Z",
    }


def test_parse_payload_can_preserve_or_reject_duplicate_targets() -> None:
    now = datetime(2026, 8, 12, 20, 0, tzinfo=timezone.utc)
    payload = {
        "request_id": "request-one",
        "target_node_ids": ["node-a", "node-a"],
        "created_at": stamp_utc(now),
        "expires_at": stamp_utc(now + timedelta(minutes=5)),
    }

    parsed = ControlCommandEnvelope.parse_payload(
        payload,
        max_lifetime=timedelta(minutes=10),
        require_unique_targets=False,
        now=now,
    )
    assert parsed.target_node_ids == ("node-a", "node-a")

    with pytest.raises(ValueError, match="malformed_targets"):
        ControlCommandEnvelope.parse_payload(
            payload,
            max_lifetime=timedelta(minutes=10),
            require_unique_targets=True,
            now=now,
        )


def test_parse_payload_rejects_reversed_lifetime() -> None:
    now = datetime(2026, 8, 12, 20, 0, tzinfo=timezone.utc)
    payload = {
        "request_id": "request-one",
        "target_node_ids": ["node-a"],
        "created_at": stamp_utc(now + timedelta(seconds=30)),
        "expires_at": stamp_utc(now + timedelta(seconds=10)),
    }

    with pytest.raises(ValueError, match="expired_or_invalid_request"):
        ControlCommandEnvelope.parse_payload(
            payload,
            max_lifetime=timedelta(minutes=10),
            require_unique_targets=False,
            now=now,
        )


def test_timestamp_helpers_require_aware_values() -> None:
    with pytest.raises(ValueError, match="malformed_timestamp"):
        stamp_utc(datetime(2026, 8, 12, 20, 0))
    with pytest.raises(ValueError, match="malformed_timestamp"):
        parse_utc_stamp("2026-08-12T20:00:00")


def test_bounded_json_and_correlation_id_are_deterministic() -> None:
    ensure_bounded_json({"value": "ok"}, max_bytes=64, error_code="too_large")
    with pytest.raises(ValueError, match="too_large"):
        ensure_bounded_json({"value": "x" * 100}, max_bytes=32, error_code="too_large")

    assert correlated_event_request_id("report", "request-one", "node-a") == (
        correlated_event_request_id("report", "request-one", "node-a")
    )
    assert len(correlated_event_request_id("report", "request-one", "node-a")) <= 64
''', encoding="utf-8")

          compatibility = Path(
              "catalog/flask_app/tests/test_federation_control_command_compatibility.py"
          )
          if compatibility.exists():
              raise SystemExit("compatibility test already exists")
          compatibility.write_text('''from __future__ import annotations

from datetime import datetime, timedelta, timezone

import pytest

from catalog.federation.software_update import APPROVED_BRANCH, APPROVED_REPOSITORY
from catalog.flask_app.services.federation_capability_requests import (
    EVENT_SCHEMA as CAPABILITY_SCHEMA,
    request_payload,
    validate_request_payload,
)
from catalog.flask_app.services.federation_update_events import (
    EVENT_SCHEMA as UPDATE_SCHEMA,
    command_payload,
    validate_command_payload,
)

TARGET = "a" * 40


def test_existing_control_command_wire_shapes_are_preserved() -> None:
    now = datetime.now(timezone.utc)
    update = command_payload(
        request_id="update-one",
        target_commit=TARGET,
        target_node_ids=("node-a", "node-a", "node-b"),
        created_at=now,
        expires_at=now + timedelta(minutes=10),
    )
    capability = request_payload(
        request_id="capability-one",
        target_node_ids=("node-a", "node-a", "node-b"),
        created_at=now,
        expires_at=now + timedelta(minutes=10),
    )

    assert update == {
        "schema": UPDATE_SCHEMA,
        "request_id": "update-one",
        "repository": APPROVED_REPOSITORY,
        "branch": APPROVED_BRANCH,
        "target_commit": TARGET,
        "target_node_ids": ["node-a", "node-b"],
        "created_at": update["created_at"],
        "expires_at": update["expires_at"],
    }
    assert capability == {
        "schema": CAPABILITY_SCHEMA,
        "request_id": "capability-one",
        "actions": ["benchmark", "contribute"],
        "target_node_ids": ["node-a", "node-b"],
        "created_at": capability["created_at"],
        "expires_at": capability["expires_at"],
    }
    assert validate_command_payload(update) is update
    assert validate_request_payload(capability) is capability


def test_both_control_command_validators_reject_reversed_lifetimes() -> None:
    now = datetime.now(timezone.utc)
    update = command_payload(
        request_id="update-one",
        target_commit=TARGET,
        target_node_ids=("node-a",),
        created_at=now,
        expires_at=now + timedelta(minutes=10),
    )
    capability = request_payload(
        request_id="capability-one",
        target_node_ids=("node-a",),
        created_at=now,
        expires_at=now + timedelta(minutes=10),
    )
    for payload, validator in (
        (update, validate_command_payload),
        (capability, validate_request_payload),
    ):
        payload["created_at"] = (now + timedelta(seconds=30)).isoformat()
        payload["expires_at"] = (now + timedelta(seconds=10)).isoformat()
        with pytest.raises(ValueError, match="expired_or_invalid_request"):
            validator(payload)
''', encoding="utf-8")
          PY

      - name: Install focused test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt -c constraints-phase2.txt
          python -m pip install pytest ruff

      - name: Format, compile, and lint touched Python
        run: |
          python -m ruff format \
            catalog/federation/control_commands.py \
            catalog/federation/tests/test_control_commands.py \
            catalog/flask_app/services/federation_update_events.py \
            catalog/flask_app/services/federation_capability_requests.py \
            catalog/flask_app/tests/test_federation_control_command_compatibility.py
          python -m compileall -q \
            catalog/federation/control_commands.py \
            catalog/federation/tests/test_control_commands.py \
            catalog/flask_app/services/federation_update_events.py \
            catalog/flask_app/services/federation_capability_requests.py \
            catalog/flask_app/tests/test_federation_control_command_compatibility.py
          python -m ruff check --ignore TRY004 \
            catalog/federation/control_commands.py \
            catalog/federation/tests/test_control_commands.py \
            catalog/flask_app/services/federation_update_events.py \
            catalog/flask_app/services/federation_capability_requests.py \
            catalog/flask_app/tests/test_federation_control_command_compatibility.py

      - name: Run focused control-plane regressions
        run: >-
          python -m pytest -o addopts= -q
          catalog/federation/tests/test_control_commands.py
          catalog/flask_app/tests/test_federation_control_command_compatibility.py
          catalog/flask_app/tests/test_federation_capability_requests.py
          catalog/flask_app/tests/test_federation_update_runtime.py
          catalog/flask_app/tests/test_federation_update_orchestration.py
          catalog/flask_app/tests/test_federation_active_leader_runtime.py

      - name: Commit only validated source changes
        shell: bash
        run: |
          git config user.name "OpenAI refactor agent"
          git config user.email "noreply@openai.com"
          git add \
            catalog/federation/control_commands.py \
            catalog/federation/tests/test_control_commands.py \
            catalog/flask_app/services/federation_update_events.py \
            catalog/flask_app/services/federation_capability_requests.py \
            catalog/flask_app/tests/test_federation_control_command_compatibility.py
          git diff --cached --check
          git commit -m "Share Federation control-command primitives"
          git push origin HEAD:agent/federation-control-command-primitives
