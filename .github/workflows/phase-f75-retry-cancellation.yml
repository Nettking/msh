name: Phase F7.5 retry and cancellation

on:
  pull_request:
    paths:
      - "catalog/capabilities/**"
      - "catalog/relay/tests/test_phase_f75_retry_cancellation.py"
      - ".github/workflows/phase-f75-retry-cancellation.yml"
  push:
    branches: [main]
    paths:
      - "catalog/capabilities/**"
      - "catalog/relay/tests/test_phase_f75_retry_cancellation.py"
      - ".github/workflows/phase-f75-retry-cancellation.yml"

permissions:
  contents: read

concurrency:
  group: f75-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retry-cancellation:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt -c constraints-phase2.txt
          python -m pip install pytest ruff
      - name: Compile F7.5 lifecycle boundary
        run: >-
          python -m compileall
          catalog/capabilities
          catalog/relay/tests/test_phase_f75_retry_cancellation.py
      - name: F7 lifecycle, relay acceptance and affected federation tests
        run: >-
          python -m pytest -o addopts= -q
          catalog/capabilities/tests
          catalog/relay/tests/test_phase_f74_capability_dispatch.py
          catalog/relay/tests/test_phase_f75_retry_cancellation.py
          catalog/federation/tests/test_phase0.py
          catalog/federation/tests/test_phase1.py
      - name: Ruff F7.5 scope
        run: >-
          python -m ruff check
          catalog/capabilities
          catalog/relay/tests/test_phase_f74_capability_dispatch.py
          catalog/relay/tests/test_phase_f75_retry_cancellation.py
          --ignore I001,RUF022,B008,C408,PLC0206

  repository-hygiene:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Validate Compose configuration
        run: docker compose config --quiet
      - name: Validate diff hygiene
        run: git diff --check HEAD^ HEAD
