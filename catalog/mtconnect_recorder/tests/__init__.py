"""Tests for the MTConnect recorder and its canonical observation projection.

This directory is a package so its ``conftest`` helpers are imported as
``catalog.mtconnect_recorder.tests.conftest``. Several test directories in this
repository have a ``conftest.py``, and a bare ``import conftest`` binds whichever
one reached ``sys.modules`` first — which, in a full-suite run, is not
necessarily this one.
"""
