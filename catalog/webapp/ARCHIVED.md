# Archived Streamlit workspace

This directory is archived and kept only for short-term source reference.

- It is **not** part of the primary operator workflow.
- It is **not** included in the default runtime dependency path.
- The active operator UI is `catalog/flask_app`.

Use `python -m catalog.flask_app.app` and the `/playback` route for operator playback.
