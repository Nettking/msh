# Active Federation implementation plans

This directory contains the current implementation plans for the supported Federation product surface.

- [Manual Federation-wide FCP updates](manual_updates.md) — coordinator-authorized exact-commit checks and verified runtime rollouts using the existing Federation event log plus host-owned Windows/POSIX update agents. A device is successful only after rebuild, restart, required-model readiness, and running-commit verification.
