# Legacy material

This directory is reserved for superseded or uncertain historical material when it is safer to preserve than delete.

At the moment, no files have been moved here automatically. Existing ambiguous content (such as the top-level `git/` directory) remains in place to avoid breaking unknown external references.
